# План действий

1. **Реализовать обработку исключений при работе с токенами**




4. **Vite Proxy** (`vite.config.ts`):
   - В dev режиме проксирует `/api` на `localhost:8080`
   - Решает CORS проблемы в разработке
